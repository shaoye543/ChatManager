package com.shaoye.chatmanager.view;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class ChatManagerView extends View {
    private static final String TAG = "FloatManagerView";

    public ChatManagerView(Context context) {
        super(context);
    }

    public ChatManagerView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public ChatManagerView(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
